{
  "name": "@janiscommerce/serverless-plugin-remove-authorizer-permissions",
  "version": "1.0.0",
  "description": "A Serverless Plugin to remove JANIS authorizers permissions",
  "main": "lib/serverless-plugin-remove-authorizer-permissions",
  "scripts": {
    "test": "export TEST_ENV=true; mocha --exit -R nyan --recursive tests/",
    "test-ci": "nyc --reporter=html --reporter=text mocha --recursive tests/",
    "watch-test": "export TEST_ENV=true; mocha --exit -R nyan -w --recursive tests/",
    "coverage": "nyc npm test",
    "lint": "eslint lib/ tests/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/janis-commerce/serverless-plugin-remove-authorizer-permissions.git"
  },
  "author": "Janis",
  "license": "ISC",
  "homepage": "https://github.com/janis-commerce/serverless-plugin-remove-authorizer-permissions.git#readme",
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.17.3",
    "husky": "^2.4.1",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0"
  },
  "files": [
    "lib/"
  ],
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "lodash.clonedeep": "^4.5.0"
  }
}
